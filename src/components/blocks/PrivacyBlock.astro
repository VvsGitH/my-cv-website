---
import type { PrivacyBlock } from '../../content/types';
import SectionHeading from '../primitives/SectionHeading.astro';
import Rich from '../primitives/Rich.astro';

interface Props {
  block: PrivacyBlock;
}

const { block } = Astro.props;
---

{block.heading && <SectionHeading text={block.heading} />}
<p class="statement">
  <Rich text={block.statement} />
</p>
<p class="place-date">{block.place}, {block.date}</p>
<p class="signature" aria-hidden="true">{block.signature}</p>

<style>
  @layer components {
    .statement {
      margin-block-start: 0.94rem;
      line-height: 1.417;
      text-align: justify;
      color: var(--color-text);
    }

    .place-date {
      margin-block-start: 0.84rem;
      font-family: var(--font-display);
      font-weight: var(--font-weight-display);
      text-align: end;
      color: var(--color-text);
    }

    /* Spec US29: an approximation in a script face, never a scan of the
       real signature. Self-hosted Primera Signature — see ticket 13. */
    .signature {
      margin-block-start: 0.1rem;
      font-family: var(--font-signature);
      font-weight: var(--font-weight-signature);
      font-size: 1.6rem;
      line-height: 1.6;
      color: var(--color-signature);
    }
  }
</style>
