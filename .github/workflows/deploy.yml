name: Deploy to GitHub Pages

# A push to the default branch is the whole publishing action: build, capture
# the PDFs and preview images, deploy. The whys — step order, the one-off repo
# setting it assumes — are in docs/issues/cv-website/09-ci-deploy.md.

on:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm

      - run: npm ci

      # Chromium only — the capture step is the sole browser consumer. Its own
      # step and not an npm hook, per ticket 08's closing note.
      - run: npx playwright install --with-deps chromium

      - run: npm run build

      # Before the upload, so a failed capture publishes nothing.
      - run: npm run captures:render

      # After the capture, so the suite can read the PDFs it asserts about, and
      # before the upload, so a regression publishes nothing either (ticket 12).
      # The runner, not `npm test`: the `pretest` hook exists to spare a local
      # run a stale `dist`, and the two steps above have already done that work.
      - run: npx playwright test

      - uses: actions/upload-pages-artifact@v5
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v5
